<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="KitBox.Views.Form"
             Title="Set up your locker">
    <ScrollView>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Premier conteneur pour le bouton CreateanewLocker1 -->
           
            
            
            <StackLayout x:Name="labelContainer1" Grid.Row="2" Margin="10,5,10,5" />
            
            <!-- Deuxième conteneur pour les éléments dynamiques -->
            <StackLayout x:Name="labelContainer" Grid.Row="1" Margin="10,5,10,5" />
            
            <!-- Bouton Finish -->
            <Button Text="Finish" Clicked="OnFinishClicked" Grid.Row="3" Margin="10,5,10,5" VerticalOptions="End" HorizontalOptions="End" BackgroundColor="Crimson"/>
        </Grid>
    </ScrollView>
</ContentPage>